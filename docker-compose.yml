# Standalone Qdrant Vector Database
# Run: docker-compose up -d
# API: http://localhost:6333
# Dashboard: http://localhost:6333/dashboard

services:
  qdrant:
    image: qdrant/qdrant
    ports:
      - "6333:6333"
      - "6334:6334"
    volumes:
      - qdrant_storage:/qdrant/storage
    restart: unless-stopped

volumes:
  qdrant_storage:
